#!/bin/bash

set -eu
: ${DISPLAY:?}
set -x

if [ -z "$1" ]; then
  echo "`basename $0` {start|stop}"
  exit
fi

case "$1" in
  start)
    /usr/bin/Xvfb $DISPLAY -ac -screen 0 1024x768x16 +extension RANDR &
    ;;
  stop)
    killall Xvfb
    ;;
esac