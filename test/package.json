{
  "private": true,
  "devDependencies": {
    "jasmine-reporters": "1.0.1",
    "lodash": "2.4.1",
    "protractor": "5.3.2",
    "protractor-html-screenshot-reporter": "0.0.17",
    "protractor-linkuisref-locator": "1.0.0",
    "request": "2.47.0",
    "webdriver-manager": "12.1.0"
  },
  "dependencies": {
    "moment": "^2.10.6"
  },
  "scripts": {
    "test": "protractor --baseUrl http://localhost:8080",
    "webdriver-update": "webdriver-manager update",
    "webdriver-start": "webdriver-manager start"
  }
}
